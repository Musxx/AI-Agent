[project]
name = "ai-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "openai>=1.98.0",
    "requests>=2.32.5",
]

[dependency-groups]
dev = [
    "ipykernel>=6.30.0",
]
